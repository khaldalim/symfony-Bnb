{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <h1>Article</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ article.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ article.description }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ article.content }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info my-1" href="{{ path('article_index_admin') }}"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour à la liste</a>
<br>
    <a class="btn btn-success my-1" href="{{ path('article_edit', {'slug': article.slug}) }}"><i class="fa fa-pencil" aria-hidden="true"></i> Modifier</a>

    {{ include('article/_delete_form.html.twig') }}
{% endblock %}
